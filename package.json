{
  "name": "bakery-films-noir",
  "version": "0.1.0",
  "private": true,
  "description": "Bakery Films / NOIR — test harness only. Site design is LOCKED; this package adds zero runtime dependencies and touches no site files.",
  "type": "module",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  },
  "engines": {
    "node": ">=18.13"
  }
}
